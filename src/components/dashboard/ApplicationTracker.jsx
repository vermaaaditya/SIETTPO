const stages = ["Applied", "Shortlisted", "Interview", "Result"]
export default function ApplicationTracker({ applications, events, expanded }) { return <article className={`portal-card tracker ${expanded ? "tracker-expanded" : ""}`}><div className="card-heading"><div><span>Application status</span><h2>{applications.length ? "Your drive pipeline" : "No applications yet"}</h2></div></div>{applications.length ? applications.map(app => <div className="pipeline" key={app.id || app.eventId}><strong>{app.company || events.find(event => event.id === app.eventId)?.company || "Placement drive"}</strong><div className="stage-row">{stages.map((stage,index) => <div className={stages.indexOf(app.status) >= index ? "stage done" : "stage"} key={stage}><i>{index + 1}</i><span>{stage}</span></div>)}</div></div>) : <p className="empty-state">Apply to an open campus drive to begin tracking each selection stage here.</p>}</article> }
