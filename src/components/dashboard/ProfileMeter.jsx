import { AlertTriangle, CheckCircle2 } from "lucide-react"
export default function ProfileMeter({ completion, deadlineSoon }) { return <article className="portal-card profile-meter"><div className="card-heading"><div><span>Profile readiness</span><h2>{completion}% complete</h2></div><CheckCircle2 size={25} color="var(--gold)"/></div><div className="meter-track"><span style={{ width: `${completion}%` }}/></div><p>Complete your CGPA, skills, resume, phone number and LinkedIn profile to unlock all drives.</p>{completion < 100 && deadlineSoon && <div className="deadline-nudge"><AlertTriangle size={18}/><div><strong>Complete your profile</strong><br/>A registration deadline falls within the next 7 days. <a href="#profile">Update profile</a></div></div>}</article> }
